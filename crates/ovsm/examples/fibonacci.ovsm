// Calculate Fibonacci sequence up to n
$n = 10
$a = 0
$b = 1
$result = [0]

FOR $i IN [1..$n]:
    $next = $a + $b
    $a = $b
    $b = $next
    // Note: APPEND not implemented yet, so we just return the last value

RETURN $b

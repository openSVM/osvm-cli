;; Calculate factorial using LISP syntax
;; This is a more complex example showing loops and conditionals together

(do
  (define n 5)
  (define result 1)

  (if (< n 0)
      "Error: negative number"
      (do
        (for (i (range 1 (+ n 1)))
          (set! result (* result i)))
        result)))
